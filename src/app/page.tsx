export default function HomePage() {
  return (
    <main className="app-shell">
      <section className="welcome-panel" aria-labelledby="page-title">
        <p className="eyebrow">Automation Systems</p>
        <h1 id="page-title">AS CRM</h1>
        <p className="lede">Vercel and Supabase migration scaffold is ready.</p>
      </section>
    </main>
  );
}
